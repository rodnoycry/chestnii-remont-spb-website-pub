import { Metadata } from "next"

export const metadata: Metadata = {
    title: "Ремонт вторички",
}

const RemontVtorichkiPage: React.FC = () => {
    return <main>Ремонт вторички</main>
}

export default RemontVtorichkiPage
